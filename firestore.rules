rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Each anonymous user can only read/write their own memo document,
    // identified by their Firebase Auth uid.
    match /memos/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
